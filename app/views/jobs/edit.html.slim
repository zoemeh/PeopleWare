.card 
    .card-body 
        h1.card-title = t('editing job').titleize
        .card-text
            == render 'form'
            .btn-group
                => link_to t('show').titleize, @job, class: 'btn btn-primary'
                =< link_to t('back').titleize, jobs_path, class: 'btn btn-secondary'
